package Game.GameState;

import Game.CoreObjects.Location;
import Game.Units.Unit;

/**
 * Represents possible actions a player can take.
 * Putting them here for now so they are easier to locate
 */
public final class ActionUtils {

    static void move(final Unit unit, final Location targetLocation) {
        //   if (unit.canMove()){
        //do it
        // }
    }

    static void attack(final Unit attacker, final Unit target) {
        // if (unit.canAttack()){
        //resolve damage;
        // }
    }

}
