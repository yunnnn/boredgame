package UI;

import CoreObjects.GameObject;
import CoreObjects.LevelMap;

/**
 * Created by yun on 11/8/14.
 */
public class MapRenderer {

    public MapRenderer(final LevelMap map){
    }

    public MapRenderer(final GameObject gameObject){
        renderItself(gameO);
        for (final child : List<> children){
            Render(child);
        }
    }

    private MapRenderer(final GameObject gameObject){
        stuff;
    }
}
